<header>
    <div class="horizontalList">
        <div>
            <button class="menuButton" title="Meniu" onclick="openNav()">
                <img class="menuIcon" src="{{ asset('images/menu.png') }}" alt="Meniu"/>
            </button>
        </div>

        <div>
            <h1>NAGIDO</h1>
        </div>

        <div>
            <button id="searchButton" class="searchButton" title="Paieška" onclick="displaySearchInput()">
                <img id="searchImage" class="searchImage" src="{{ asset('images/search-single.png') }}" alt="Paieška"/>
            </button>
        </div>

        <div>
            <form id="searchForm" method="POST" action="{{ path('search') }}" class="searchForm">
                <input id="searchInput" type="text" name="search" id="search" placeholder="Ieškokite">
            </form>
        </div>

        <div>
            <a href="{{ asset('logout') }}">
                <button class="signOutButton" >
                    <span>Atsijungti</span>
                </button>
            </a>
        </div>
    </div>
</header>